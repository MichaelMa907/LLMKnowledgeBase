---
note_type: learned
title: "<Truth Note Title>"
learn_id: "<YYYY-MM-DD-HHmm>-<learn-slug>"
summary: "<one-line summary>"
scope: "project"
global: false
importance: 0.50
machine_id: ""
project_note: "Projects/<project-slug>/<project-slug>.md"
project_slug: ""
commit_note: "Projects/<project-slug>/Commits/<YYYY-MM-DD HHmm> - <commit-slug>.md"
category: "instruction"
source: "verification"
confidence: 0.80
created: "<YYYY-MM-DD HH:MM>"
last_updated: "<YYYY-MM-DD HH:MM>"
status: "active"
tags:
  - learned
---

# <Truth Note Title>

> Store this note in one of these scope paths:
>
> - `Machines/<machine-id>/Learned/`
> - `Projects/<project-slug>/Learned/`
>
> Use `scope`, `global`, `importance`, `confidence`, `summary`, and `machine_id` to tell the generator how this truth should surface in generated indexes.
>
> `global: true` adds the note to `Globals/globalIndex.md` but does not move the file.
>
> If a fact only applies to one machine, keep it in machine scope even if many projects depend on it.
>
> Keep one durable truth per file.
>
> Keep the body lightweight if needed; this note does not need rigid formatting as long as the truth, scope, reuse context, and links are clear.
>
> Use a live wiki link only for `Origin commit`. Keep `Origin project`, related commits, related projects, and richer provenance as plain text paths.

## Scope
- Applies at:
- Why this scope is correct:
- Promotion or demotion notes:

## Truth
- What is true:
- Why it matters:
- When to reuse it:

## Evidence
- How this was learned:
- Limits or caveats:
- Re-check trigger:

## Links
- Origin project: `Projects/<project-slug>/<project-slug>.md`
- Origin commit: `[[Projects/<project-slug>/Commits/<YYYY-MM-DD HHmm> - <commit-slug>]]` in the template only; instantiated learned notes should use one bare `[[...]]` origin-commit link
- Related notes:
- Related files or references:

## Sources
- 
